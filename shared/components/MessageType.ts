/* eslint-disable no-unused-vars */
enum MessageType {
    CONNECT,
    DISCONNECT,
    JOIN,
    LEAVE,
    INPUTS,
    UPDATE,
    PING,
    PONG
}

export default MessageType;
export { MessageType };

